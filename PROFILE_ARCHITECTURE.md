# Profile Information Architecture

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER REGISTRATION                        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
                    ┌────────────────┐
                    │  Registration  │
                    │     Form       │
                    │                │
                    │  • First Name  │
                    │  • Last Name   │
                    │  • Email       │
                    │  • Password    │
                    └────────┬───────┘
                             │
                             ▼
                    ┌────────────────┐
                    │  Auth Server   │
                    │  signUp()      │
                    └────────┬───────┘
                             │
                             ▼
                    ┌────────────────┐
                    │  Users Table   │
                    │  (Database)    │
                    │                │
                    │  id            │
                    │  email         │
                    │  first_name ✓  │
                    │  last_name  ✓  │
                    │  password_hash │
                    └────────┬───────┘
                             │
                             │ PERMANENT STORAGE
                             │ (Cannot be modified)
                             │
┌────────────────────────────┴────────────────────────────────────┐
│                      PROFILE DISPLAY FLOW                        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
                    ┌────────────────┐
                    │  Settings Page │
                    │  (Profile Tab) │
                    └────────┬───────┘
                             │
                             ▼
                    ┌────────────────┐
                    │  GET /api/     │
                    │  settings      │
                    └────────┬───────┘
                             │
                             ▼
                    ┌────────────────┐
                    │  Query Users   │
                    │  Table         │
                    │                │
                    │  SELECT        │
                    │  first_name,   │
                    │  last_name,    │
                    │  email         │
                    └────────┬───────┘
                             │
                             ▼
                    ┌────────────────┐
                    │  Return Data   │
                    │  to Frontend   │
                    └────────┬───────┘
                             │
                             ▼
                    ┌────────────────┐
                    │  Display as    │
                    │  READ-ONLY     │
                    │                │
                    │  🔒 First Name │
                    │  🔒 Last Name  │
                    │  🔒 Email      │
                    └────────────────┘
```

---

## Data Flow Layers

### Layer 1: Database (Permanent Storage)
```
┌─────────────────────────────────────┐
│         USERS TABLE                 │
├─────────────────────────────────────┤
│ id          UUID PRIMARY KEY        │
│ email       VARCHAR(255) UNIQUE     │
│ first_name  VARCHAR(255) ← NEW     │
│ last_name   VARCHAR(255) ← NEW     │
│ password_hash TEXT                  │
│ created_at  TIMESTAMP               │
└─────────────────────────────────────┘
         ↑
         │ Written ONCE during registration
         │ READ ONLY after that
         │
```

### Layer 2: API (Protection Layer)
```
┌─────────────────────────────────────┐
│      GET /api/settings              │
├─────────────────────────────────────┤
│ ✓ Retrieves from users table       │
│ ✓ Returns firstName, lastName,     │
│   email as read-only data           │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│      POST /api/settings             │
├─────────────────────────────────────┤
│ ✓ Filters out firstName, lastName, │
│   email from updates                │
│ ✓ Only saves other settings         │
│ ✗ Rejects profile field updates    │
└─────────────────────────────────────┘
```

### Layer 3: UI (Display Layer)
```
┌─────────────────────────────────────┐
│      Settings Page (Profile Tab)    │
├─────────────────────────────────────┤
│ ℹ️  "This information was provided  │
│    during registration and cannot   │
│    be modified."                    │
│                                     │
│ First Name:  [John          ] 🔒   │
│ Last Name:   [Doe           ] 🔒   │
│ Email:       [john@email.com] 🔒   │
│                                     │
│ (No Save button)                    │
└─────────────────────────────────────┘
```

---

## Security Architecture

```
┌─────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                       │
└─────────────────────────────────────────────────────────┘

Layer 1: UI Protection
├─ Input fields are disabled
├─ readOnly attribute set
├─ No onChange handlers
└─ No save button

Layer 2: API Protection
├─ Filters out profile fields from POST
├─ Validates incoming data
├─ Rejects unauthorized updates
└─ Returns error for invalid requests

Layer 3: Database Protection
├─ Data stored in users table
├─ No update triggers for profile fields
├─ Row-level security policies
└─ Audit logging (if enabled)

Result: Multi-layer defense against unauthorized changes
```

---

## Comparison: Before vs After

### BEFORE (Editable)
```
Registration Form
      ↓
Settings (editable)
      ↓
Stores.settings JSON
      ↓
Can be modified anytime
      ↓
❌ No data integrity
```

### AFTER (Read-Only)
```
Registration Form
      ↓
Users Table (permanent)
      ↓
Settings (read-only display)
      ↓
Cannot be modified
      ↓
✅ Data integrity guaranteed
```

---

## Component Interaction Diagram

```
┌──────────────┐
│ Registration │
│    Page      │
└──────┬───────┘
       │
       │ POST /api/auth/register
       │ { firstName, lastName, email, password }
       │
       ▼
┌──────────────┐
│ Auth Server  │
│  signUp()    │
└──────┬───────┘
       │
       │ INSERT INTO users
       │ (first_name, last_name, email, ...)
       │
       ▼
┌──────────────┐
│ Users Table  │
│  (Database)  │
└──────┬───────┘
       │
       │ Data stored permanently
       │
       ▼
┌──────────────┐
│  Dashboard   │
│  (User logs  │
│   in later)  │
└──────┬───────┘
       │
       │ Navigate to Settings
       │
       ▼
┌──────────────┐
│  Settings    │
│    Page      │
└──────┬───────┘
       │
       │ GET /api/settings
       │
       ▼
┌──────────────┐
│ Settings API │
└──────┬───────┘
       │
       │ SELECT first_name, last_name, email
       │ FROM users WHERE id = ?
       │
       ▼
┌──────────────┐
│ Users Table  │
└──────┬───────┘
       │
       │ Return data
       │
       ▼
┌──────────────┐
│  Settings    │
│  Page (UI)   │
│              │
│  Display as  │
│  READ-ONLY   │
└──────────────┘
```

---

## State Management Flow

```
┌─────────────────────────────────────┐
│     Component State (Frontend)      │
├─────────────────────────────────────┤
│ const [settings, setSettings] =     │
│   useState({                        │
│     firstName: '',  ← From API      │
│     lastName: '',   ← From API      │
│     email: '',      ← From API      │
│     phone: '',      ← Editable      │
│     ...otherSettings                │
│   })                                │
└─────────────────────────────────────┘
         ↑                    │
         │                    │
    GET  │                    │ POST (filtered)
         │                    │
         │                    ▼
┌─────────────────────────────────────┐
│          API Layer                  │
├─────────────────────────────────────┤
│ GET:  Returns all settings          │
│       (profile from users table)    │
│                                     │
│ POST: Filters out profile fields    │
│       Saves only other settings     │
└─────────────────────────────────────┘
         ↑                    │
         │                    │
    Read │                    │ Write (filtered)
         │                    │
         │                    ▼
┌─────────────────────────────────────┐
│        Database Layer               │
├─────────────────────────────────────┤
│ users table:                        │
│   - first_name (READ ONLY)          │
│   - last_name (READ ONLY)           │
│   - email (READ ONLY)               │
│                                     │
│ stores.settings JSON:               │
│   - phone (EDITABLE)                │
│   - other settings (EDITABLE)       │
└─────────────────────────────────────┘
```

---

## Summary

**Architecture Principles:**
1. ✅ Separation of concerns (profile vs settings)
2. ✅ Single source of truth (users table)
3. ✅ Multi-layer protection (UI, API, DB)
4. ✅ Clear data flow (registration → storage → display)
5. ✅ Immutable profile data (write once, read many)

**Result:** Robust, secure, permanent read-only profile system.
